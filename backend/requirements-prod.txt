# Production-only optimized requirements
# This includes the same packages as requirements.txt but with production optimizations

# Include all core requirements
-r requirements.txt

# Production-specific packages
gunicorn==21.2.0
newrelic==9.2.0  # Application monitoring